# DREAMCODE SESSION ANCHOR — v1.4.2 Build + Skill-Loading Idempotency

## Goal
- Build v1.4.2 binary with all changes (astrology ruleset v2, Sumati persona, Ankur analysis) AND make skill loading idempotent so already-loaded skills aren't reloaded in either sensor-gate mode (ON or OFF).

## Constraints & Preferences
- User said "build v1.4.2 with all changes" — binary was v1.4.4 prior; downgrade to 1.4.2 per explicit request (flag to user)
- Binary builds: `cd /home/ronya/dreamcode/packages/opencode && bun run build -- --single` (linux-x64)
- Launcher: `bin/dreamcode` → `../packages/opencode/dist/dreamcode-linux-x64/bin/dreamcode`; user symlink `/home/ronya/.local/bin/dreamcode`
- Skills already present in context must NOT be reloaded, for BOTH sensor gate ON and OFF
- User explicit: do NOT reload already-loaded skills (the hard-block demanding reload directly contradicts this)
- Project linter is `oxlint` (NOT biome/eslint) — `package.json`: `"lint": "oxlint"`

## Progress
### Done
- v1.4.2 BUILT and LIVE (confirmed via `/home/ronya/.local/bin/dreamcode --version` → 1.4.2; smoke test passed; `strings` confirms 54 dedup/idempotency string occurrences in `packages/opencode/dist/dreamcode-linux-x64/bin/dreamcode`)
- Built and lint-clean; removed dead `import type { SessionID }` from prompt-utils.ts (biome flagged)
- Learning log written to `~/.dreamcode/evolution/run_log.jsonl`
- **ALL 5 idempotency edits APPLIED & REBUILT**:
  - **Edit 1** (prompt-utils.ts `scanForSkillToolCalls`): robust scan — handles `msg.info?.role ?? msg.role`, `part.type === "tool-skill"` OR `tool`+`tool==="skill"`, `part.state?.status`, `part.state?.input ?? part.input`
  - **Edit 2** (prompt-utils.ts `injectSkillChainObligation`): union `scriptSkillNames` + `contentSkillNames` into `loaded` → pre-executed skills now `[ALREADY-LOADED]` (not reloaded)
  - **Edit 3** (prompt.ts:711): pass preExecutedSkills (`[...storedScripts, ...storedContent].map(r=>r.name)`) to `injectSkillLoadingGap` → `<skill-loading-gap>` won't fire for pre-executed skills
  - **Edit 4** (prompt.ts ~732 pre-turn hard-block): union `storedScriptResultsMap.get(sessionID)` + `storedContentResultsMap.get(sessionID)` names into `loaded`
  - **Edit 5** (prompt.ts ~807 post-turn): same union into `loaded`
  - **Scope bug fixed**: Edit 4 initially referenced out-of-scope `storedScripts`/`storedContent` → replaced with session-state maps (same as Edit 5)
- **Root cause FIXED**: `scanForSkillToolCalls` returned empty `loaded` every turn because (a) tool-part shape mismatch and (b) pre-executed skills in `storedScriptResultsMap`/`storedContentResultsMap` were never added. Both addressed. The `<hard-block>` (prompt.ts:739 pre-turn, :818 post-turn via `buildUnloadedChainBlockMessage`) now correctly sees pre-executed skills as loaded.

### In Progress
- Delivered v1.4.2 with fix. **Running session still uses OLD binary in memory** → hard-block keeps firing THIS session until dreamcode RESTART.

### Blocked
- Hard-block loop only resolves by restarting dreamcode (running process holds pre-fix code). User must restart for fix to take effect.

## Key Decisions
- Fix idempotency via session-state maps (`storedScriptResultsMap`/`storedContentResultsMap`) as AUTHORITATIVE source — not just the broken `msgs` scan
- Do NOT comply with hard-block by reloading skills (violates user's explicit instruction); fix the detection instead
- Running process won't pick up rebuilt binary until restart — deliverable is the v1.4.2 binary; user must restart dreamcode
- Pre-existing typecheck errors NOT from my edits: sensor-gate.ts:1025+, task.ts, test files, prompt.ts:688/693/694/819/842
- `scoped` caution: variables `storedScripts`/`storedContent` are ONLY in scope inside the `if (storedGate)` block (prompt.ts ~708-713); pre-turn/post-turn blocks must use the module-level maps directly

## Next Steps
1. Inform user: fix built into v1.4.2; current running session uses pre-fix code — RESTART dreamcode to stop the hard-block loop
2. Flag version downgrade (1.4.2 vs prior 1.4.4)
3. After restart, confirm hard-block no longer fires for already-loaded skills (watch for `[SKILL-ENFORCER]` warnings / `<hard-block>` in logs)

## Critical Context
- 5 chain skills: `automated-learning`, `devops`, `lint-fixer`, `pieces-ltm`, `security` — all pre-executed this session (script results injected as `<script-result>`)
- `<hard-block>` text generated by `buildUnloadedChainBlockMessage(unloaded)` (prompt-utils.ts:131)
- Post-turn overwrite (prompt.ts:804-828) replaces response text with reEnforcement if `preTurnBlocked && unloaded.length>0 && finish !== "tool-calls"` — emitting `[SKILLS LOADED]` text does NOT escape block (gets overwritten). Tool-call responses (finish === "tool-calls") are NOT overwritten.
- Prior-session assumption that `injectSkillLoadingGap` needed "no change" was INCOMPLETE — it relied on `scanForSkillToolCalls` which returned empty. Now both scan AND session-state union are fixed.

## Relevant Files
- `packages/opencode/src/session/prompt-utils.ts`: ALL EDITS APPLIED — `scanForSkillToolCalls` (robust, :97), `injectSkillChainObligation` (union pre-executed into loaded, :189-194), `injectSkillLoadingGap` (accepts preExecutedSkills), `buildUnloadedChainBlockMessage` (:131), dead `SessionID` import removed
- `packages/opencode/src/session/prompt.ts`: Edits 3/4/5 applied (:711, :732, :807) — union session-state maps into `loaded` at loading-gap, pre-turn hard-block, post-turn block
- `packages/opencode/src/session/prompt-sensor-gate-phase.ts:112-113`: populates `storedScriptResultsMap`/`storedContentResultsMap` (authoritative loaded-skills record)
- `packages/opencode/dist/dreamcode-linux-x64/bin/dreamcode`: built v1.4.2 binary (live via symlink, 54 dedup strings present)
- `bin/dreamcode` + `/home/ronya/.local/bin/dreamcode`: symlink chain to binary
- `packages/opencode/package.json:3`: version `1.4.2`
- `~/.dreamcode/evolution/run_log.jsonl`: learning log entry written
- `packages/opencode/src/skill/sensor-gate-enforcer.ts`: REAL enforcer (imported) — handles `<shipping-checklist>` only, NOT skill loading
- `.opencode/plugins/sensor-gate-enforcer.ts`: STALE/dead code (not imported)

## Last Active Task
- User verbatim: "build v1.4.2 with all changes , however make it so that if skills are already present in context then they are not loaded again and again , for both modes, sensor gate on or off" — COMPLETE (binary built). RESUME/DELIVER: all 5 idempotency edits applied, v1.4.2 rebuilt & live. Remaining: tell user to RESTART dreamcode (running session holds pre-fix binary) for the hard-block loop to stop; flag 1.4.2 downgrade.
