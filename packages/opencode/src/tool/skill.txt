Load a specialized skill when the sensor gate specifies a skill chain. Use this tool to inject the skill's instructions and resources into current conversation. The output may contain detailed workflow guidance as well as references to scripts, files, etc in the same directory as the skill.

You MUST load each skill from the chain using this tool. The skill name must match one of the skills listed in the sensor gate's skill chain. After loading all chain skills, acknowledge with [SKILLS LOADED].
